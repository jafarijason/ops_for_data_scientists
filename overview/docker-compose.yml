version: '3.8'
services:
    ml_fastapi_overview:
        image: jafarijason/ml_fastapi_overview:1.0.10
        build:
            context: .
            dockerfile: ./Dockerfile
        ports:
          - 8100:8000
